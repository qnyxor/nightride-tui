# Third-Party Licenses

`nightride-tui` is distributed under the Apache-2.0 license. The release
binary statically links additional crates governed by the licenses
listed here. The full text of each unique license appears below; every
bundled crate is attributed to its license through the index.

## Overview

| License | Crates |
|---------|--------|
{{#each overview}}
| [{{name}}](#{{id}}) | {{count}} |
{{/each}}

## Crates by license

{{#each licenses}}
<a id="{{id}}"></a>

### {{name}}

Used by:

{{#each used_by}}
- [{{crate.name}} {{crate.version}}]({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}})
{{/each}}

```text
{{text}}
```

{{/each}}

---

Generated by `cargo about` from `Cargo.lock`. Regenerate via `make notice`.
